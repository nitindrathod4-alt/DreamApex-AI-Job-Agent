import React,{useState} from "react";
import {createRoot} from "react-dom/client";
import "./styles.css";
const API="http://localhost:8000";
function App(){const[resume,setResume]=useState("");const[job,setJob]=useState("");const[result,setResult]=useState(null);const[loading,setLoading]=useState(false);async function analyze(){setLoading(true);const r=await fetch(`${API}/api/jobs/match`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({resume_text:resume,job_title:"DevOps / Cloud Engineer",job_description:job})});setResult(await r.json());setLoading(false)}return <div className="app"><header><div className="logo">D</div><div><h1>DreamApex AI</h1><p>AI Job Search & Application Agent</p></div></header><section className="hero"><span className="badge">AGENTIC AI</span><h2>Find better jobs.<br/>Apply smarter.</h2><p>Analyze your profile, match jobs, identify skill gaps and prepare for interviews with AI.</p></section><section className="grid"><div className="card"><h3>Your Resume / Skills</h3><textarea value={resume} onChange={e=>setResume(e.target.value)} placeholder="Paste your resume text or skills here..."/></div><div className="card"><h3>Job Description</h3><textarea value={job} onChange={e=>setJob(e.target.value)} placeholder="Paste a job description here..."/></div></section><button onClick={analyze} disabled={loading||!resume||!job}>{loading?"Analyzing...":"Analyze Job Match"}</button>{result&&<section className="result"><div className="score">{result.match_score}%<small>Match</small></div><div><h3>{result.recommendation}</h3><p><b>Matched:</b> {result.matched_skills.join(", ")||"None detected"}</p><p><b>Missing:</b> {result.missing_skills.join(", ")||"None detected"}</p></div></section>}<footer>DreamApex AI • Human approval required before final application submission</footer></div>}
createRoot(document.getElementById("root")).render(<App/>);
